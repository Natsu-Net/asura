# Asura Manga API Environment Variables

# Deno KV Access Token (required for production Deno Deploy)
# Get this from your Deno Deploy dashboard under "Settings" > "Access Tokens"
DENO_KV_ACCESS_TOKEN=your_deno_kv_access_token_here

# Application Base URL (optional)
# Used for generating absolute URLs in API responses
APP_URL=https://your-deploy-url.deno.dev

# Cron Secret (optional)
# Additional security for cron job endpoints
CRON_SECRET=your_secret_key_here

# Environment (optional)
# Helps distinguish between development and production
DENO_ENV=production

# Deployment ID (automatically set by Deno Deploy)
# DENO_DEPLOYMENT_ID=auto_generated_by_deno_deploy
